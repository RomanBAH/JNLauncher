<Window x:Class="JNightLauncher.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="JNight Launcher" Height="560" Width="700"
        WindowStyle="None" ResizeMode="NoResize"
        Background="#272b30" FontFamily="Segoe UI"
        WindowStartupLocation="CenterScreen"
        Icon="favicon.ico"
        >

    <!-- Тут вставляем ресурсы, включая стиль кнопок -->
    <Window.Resources>
        <Style TargetType="Button" x:Key="CustomButtonStyle">
            <Setter Property="Background" Value="#3E444C"/>
            <Setter Property="Foreground" Value="#c8c8c8"/>
            <Setter Property="BorderBrush" Value="#191B1E"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="2">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#272B30"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#1E1E1E"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="BorderBrush" Value="#555"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="#222"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="SelectionBrush" Value="#00ACEB"/>
            <Setter Property="SelectionOpacity" Value="0.5"/>
            <Setter Property="CaretBrush" Value="White"/>

            <Style.Triggers>
                <Trigger Property="IsKeyboardFocused" Value="True">
                    <Setter Property="BorderBrush" Value="#777"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    
    <DockPanel LastChildFill="True">

        <!-- Кастомный заголовок -->
        <Grid Height="30" DockPanel.Dock="Top" Background="#272b30"
      MouseLeftButtonDown="Header_MouseLeftButtonDown">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="30"/>
                <ColumnDefinition Width="30"/>
            </Grid.ColumnDefinitions>

            <!-- Заголовок -->
            <TextBlock Text="JNight Launcher" Foreground="#c8c8c8" VerticalAlignment="Center"
               Margin="10,0" Grid.Column="0"/>

            <!-- Кнопка свернуть -->
            <Button Content="—"
            Width="30" Height="30"
            Style="{StaticResource CustomButtonStyle}"
            Click="MinimizeButton_Click"
            Grid.Column="1"/>

            <!-- Кнопка закрыть -->
            <Button Content="X"
            Width="30" Height="30"
            Style="{StaticResource CustomButtonStyle}"
            Click="CloseButton_Click"
            Grid.Column="2"/>
        </Grid>



        <!-- Основной контент -->
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="300"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Левая колонка -->
            <StackPanel x:Name="LeftPanel" Grid.Column="0" Background="#272b30" Margin="10">
                <Image x:Name="LogoImage" Height="100" Margin="0,0,0,10" Source="logo.png" 
                       MouseLeftButtonUp="WebsiteLink_Click"
                       Cursor="Hand"/>

                <Label Content="Ник" Foreground="#c8c8c8" FontSize="12" HorizontalAlignment="Center"/>
                <TextBox x:Name="NameBox" FontSize="12" Margin="10,5,10,10" 
                     Background="#3E444C" Foreground="#c8c8c8"
                     BorderBrush="#191B1E" BorderThickness="1" TextChanged="NameBox_TextChanged"/>
                <Label Content="Подключиться к серверу" Foreground="#c8c8c8" FontSize="12" Margin="0,10,0,5" HorizontalAlignment="Center"/>
                <StackPanel x:Name="ServersPanel"/>
            </StackPanel>

            <!-- Правая колонка: HTML -->
            <WebBrowser x:Name="RightBrowser" Grid.Column="1" Margin="0" 
                        Navigating="RightBrowser_Navigating"/>
        </Grid>
    </DockPanel>

</Window>
